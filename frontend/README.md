# frontend

Reserved for the tiny-link web UI (**React**). Not implemented yet.

The current implementation plans (`docs/superpowers/plans/00`–`07`) cover the
backend API only. When the UI is built, the React single-page app lives here and
talks to the backend over the REST contract described in [`docs/api.md`](../docs/api.md).
